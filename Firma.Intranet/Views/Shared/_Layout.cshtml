@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Intranet</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

    <style>
        body {
            min-height: 100vh;
            display: flex;
        }

        .sidebar {
            width: 240px;
            background-color: #343a40;
            color: white;
            flex-shrink: 0;
            align-items:center;
        }

        .sidebar a {
            color: white;
            padding: 12px 16px;
            margin-top: 5px;
            display: block;
            text-decoration: none;
        }

        .sidebar a:hover {
            background-color: #495057;
        }

        .topbar {
            height: 60px;
            background-color: #f8f9fa;
            padding: 10px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid #ddd;
        }

        .main-content {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .content-area {
            padding: 20px;
            background-color: #f5f5f5;
            min-height: calc(100vh - 60px);
        }
    </style>
</head>
<body>

    <div class="sidebar">
        <h4 class="text-center py-3">Intranet</h4>
        <a href="@Url.Action("Index", "Home")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-home"></i> Strona główna</a>
        <a href="@Url.Action("Index", "Dostawa")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-truck"></i> Dostawy</a>
        <a href="@Url.Action("Index", "Fakturas")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-file-invoice"></i> Faktury</a>
        <a href="@Url.Action("Index", "Klient")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-user"></i> lienci</a>
        <a href="@Url.Action("Index", "Pracownik")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-users"></i> Pracownicy</a>
        <a href="@Url.Action("Index", "Produkt")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-box-open"></i> Produkty</a>
        <a href="@Url.Action("Index", "Recenzja")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-star"></i> Recenzje</a>
        <a href="@Url.Action("Index", "Rezerwacja")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-calendar-check"></i> Rezerwacje</a>
        <a href="@Url.Action("Index", "StronaCMS")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-file-alt"></i> Zarządzanie treścią</a>
        <a href="@Url.Action("Index", "Kategoria")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-tags"></i> Kategorie</a>
        <a href="@Url.Action("Index", "UstawieniaPortalu")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-cog"></i> Ustawienia portalu</a>
        <a href="@Url.Action("Index", "Wiadomosci")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-envelope"></i> Wiadomości</a>
        
    </div>


    <div class="main-content">
        <div class="topbar">
            <div>
                <strong>Zalogowany:</strong> Jan Kowalski
            </div>
            <div>
                <span id="clock" class="me-4"></span>
                <a href="@Url.Action("Index", "Wiadomosc")" class="btn btn-outline-secondary btn-sm me-2"><i class="fas fa-envelope"></i></a>
                <a href="/Logout" class="btn btn-outline-danger btn-sm">Wyloguj</a>
            </div>
        </div>

        <div class="content-area">
            @RenderBody()
            @RenderSection("Scripts", required: false)
        </div>
    </div>

    <script>
        function updateClock() {
            const now = new Date();
            const timeString = now.toLocaleTimeString('pl-PL');
            document.getElementById('clock').textContent = timeString;
        }
        setInterval(updateClock, 1000);
        updateClock();
    </script>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
