@model IEnumerable<Firma.Data.Models.UstawieniaPortalu>

@{
    ViewData["Title"] = "Ustawienia portalu";
    Layout = "~/Views/Shared/_IntranetTableLayout.cshtml";
}

@section Tabela{
    <table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>
                Godziny pracy
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kontakt)
            </th>
            <th>
                Adres firmy
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GodzinyPracy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Kontakt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DaneFirmy)
            </td>
            <td>
                <div>
                    <a asp-action="Edit" asp-route-id="@item.Id" style="margin-right: 5px; text-decoration: none;">
                        <button type="button" class="btn btn-primary">Edytuj</button>
                    </a>

                    <a asp-action="Delete" asp-route-id="@item.Id">
                        <button type="button" class="btn btn-danger">Usuń</button>
                    </a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
}